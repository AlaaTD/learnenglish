# تقرير مراجعة وتطوير قسم الجرامر — English90

**النطاق:** حقل `grammar` في `content/day-01.json` حتى `content/day-50.json` (الأيام 51-90 غير موجودة بعد وخارج نطاق هذه المراجعة).
**التاريخ:** 2026-09-22

---

## 1. ملخص تنفيذي

تمت مراجعة شاملة لكل بيانات الجرامر في الأيام الخمسين الأولى، بمقارنة كل حقل فعلي في ملفات `content/*.json` مع:
- المخطط الذي تقرأه الواجهة فعلياً (`src/lib/queries.ts` → `seed.mjs` → SQLite → `grammar-academy.tsx` / `grammar-quiz.ts`)
- توثيق الصيغة الرسمية (`content/FORMAT.md`)
- سكريبت الفحص الآلي (`scripts/validate-content.mjs`)

**النتيجة الأهم:** سكريبت `validate-content.mjs` كان يعطي **PASS** على الأيام 1-50 طوال الوقت رغم وجود 4 مشاكل بنيوية حقيقية، لأنه لا يتحقق من أسماء الحقول الفرعية داخل كل درس نحوي (`label`, `note`, `titleArabic`, `commonUsage`) — فقط من وجود الحقول الرئيسية وعددها. أي أن المحتوى كان "صحيحاً" من ناحية آلية الفحص، لكنه كان **يظهر ناقصاً أو فارغاً فعلياً في واجهة المستخدم**.

---

## 2. المشاكل التي تم اكتشافها (قبل أي تعديل)

### المشكلة 1 — مفتاح خاطئ في `commonMistakes` (الأيام 4-10)
كانت عناصر `commonMistakes` تستخدم المفتاح `"explanation"` بدلاً من `"note"` الذي تقرأه الواجهة فعلياً (`src/lib/queries.ts` يقرأ `note` حصرياً). النتيجة: بطاقة "خطأ شائع" في تطبيق الجرامر كانت تظهر بدون أي تفسير — فقط الجملة الخاطئة والصحيحة بدون توضيح السبب.

### المشكلة 2 — شارة `structures` فارغة في كل الأيام 4-50
كل عنصر في مصفوفة `structures` كان يحتوي على `"explanation"` / `"explanationArabic"` بدلاً من `"label"` الذي تعرضه الشارة الصغيرة (badge) بجانب كل نمط تركيبي في الواجهة. النتيجة: الشارة كانت تظهر **فارغة تماماً** في 52 درساً نحوياً، والنص التوضيحي الإنجليزي (`explanation` الأصلي) لم يكن يُعرض إطلاقاً لأن الواجهة لا تقرأ هذا المفتاح من الأساس.

### المشكلة 3 — غياب `commonUsage` تماماً (الأيام 11-27)
20 درساً نحوياً عبر 17 ملفاً (الأيام 11 حتى 27) لم يكن بها حقل `commonUsage` إطلاقاً — القسم الذي يعرض أمثلة/قواعد استخدام إضافية في الواجهة. الأيام 1-10 كانت تحتويه، والأيام 28-50 كانت تحتوي محتوى مكافئاً لكن تحت مفتاح خاطئ (انظر المشكلة 4)، بينما الفجوة الحقيقية والكاملة كانت في منتصف المنهج بالضبط (11-27).

### المشكلة 4 — حقلا `rules` / `rulesArabic` ميتان تماماً (الأيام 28-50)
24 درساً نحوياً عبر 23 ملفاً (الأيام 28-50) كانا يحتويان محتوى تعليمياً حقيقياً وجيداً تحت مفتاحي `rules` و`rulesArabic`، لكن هذين المفتاحين **غير معرّفين إطلاقاً** في `src/lib/queries.ts`، وبالتالي:
- `seed.mjs` لا يقرأهما عند حقن البيانات في قاعدة البيانات.
- لا يصلان أبداً إلى الواجهة.
- كل هذا المحتوى (98 عنصراً) كان بيانات ميتة 100% — مكتوبة بعناية لكن غير مرئية للمستخدم النهائي أبداً.

في نفس الأيام (28-50)، كان حقل `titleArabic` **غائباً تماماً**، فكان العنوان الثنائي اللغة للدرس النحوي يظهر بالإنجليزية فقط في الواجهة رغم أن كل الأيام الأخرى (1-27) لها عنوان عربي.

> **ملاحظة منهجية:** `validate-content.mjs` لا يكتشف أياً من المشاكل الأربع أعلاه لأنه يتحقق فقط من وجود `title` و`explanation` كحقول إلزامية على مستوى الدرس، ومن وجود `pattern` داخل `structures`، دون التحقق من `label`, `note`, `titleArabic`, أو `commonUsage`. الفحص الآلي كان يعطي PASS مستمراً طوال هذه المشاكل.

---

## 3. الإصلاحات والإضافات المنفذة

### 3.1 إصلاح `commonMistakes.explanation → note` (الأيام 4-10)
تم تحويل المفتاح في 16 عنصر عبر الأيام 4-10 (اليوم 4: 4 عناصر، الأيام 5-10: عنصران لكل يوم) من `explanation` إلى `note` دون تغيير أي نص آخر. تم التحقق بـ `git diff` أن التعديل محصور في اسم المفتاح فقط.

### 3.2 إضافة `label` ثنائي اللغة لكل `structures` (الأيام 4-50)
تمت إضافة 150 قيمة `label` (بصيغة "English · عربي") لكل بنية تركيبية عبر 52 درساً نحوياً في 47 ملفاً، مبنية بدقة على مضمون كل `pattern`/`explanation` الفعلي لكل درس على حدة (وليست قيماً عامة مكررة). لم يُحذف أي حقل موجود مسبقاً.

### 3.3 ترحيل `rules`/`rulesArabic` الميتة إلى `commonUsage` الفعلي (الأيام 28-50)
تم نقل 98 عنصراً عبر 24 درساً في 23 ملفاً من الحقلين الميتين إلى `commonUsage` الحقيقي المقروء من الواجهة، بصياغة ثنائية اللغة، مع حذف `rules`/`rulesArabic` نهائياً من كل الملفات. تم التأكد عبر `grep` من عدم بقاء أي أثر لهذين الحقلين في `content/`.

### 3.4 تأليف `commonUsage` جديد بالكامل (الأيام 11-27)
تمت كتابة 65 عنصر `commonUsage` جديداً عبر 20 درساً نحوياً في 17 ملفاً، تغطي بالضبط الفجوة الموصوفة في المشكلة 3. بما أن كل درس في هذا النطاق يحتوي أصلاً على 3 أمثلة جاهزة في `examples`، تم تصميم محتوى `commonUsage` ليقدم قيمة إضافية حقيقية بدل تكرار الأمثلة: قواعد استخدام عملية، كلمات إشارة (signal words)، فروقاً دقيقة بين تراكيب متشابهة، وملاحظات ترتيب الكلمات — بنفس الأسلوب الثري المستخدم في الأيام 28-50 بعد ترحيلها.

### 3.5 تأليف `titleArabic` جديد بالكامل (الأيام 28-50)
تمت كتابة عنوان عربي لكل من 24 درساً نحوياً عبر 23 ملفاً (الأيام 28-50)، بنفس أسلوب العناوين الثنائية المستخدمة في الأيام 1-27 (مصطلح نحوي عربي، أحياناً مع المصطلح الإنجليزي بين قوسين، متبوعاً بوصف وظيفي مختصر).

---

## 4. التحقق النهائي

1. **`node scripts/validate-content.mjs --days 1-50`** → `PASS — 0 error(s), 0 warning(s)` بعد كل خطوة وفي الفحص النهائي.
2. **فحص بنيوي مخصص** عبر الأيام 1-50 وكل الدروس النحوية (74 درساً) يتحقق من: وجود وسلامة `title`, `titleArabic`, `explanation`, `explanationArabic`؛ أن كل عنصر `structures` له `label` و`pattern`؛ وجود 3 أمثلة فأكثر؛ `commonUsage` غير فارغ وكل عنصر نص صالح؛ كل عنصر `commonMistakes` له `wrong`/`right`/`note`؛ وعدم وجود أي بقايا لحقلي `rules`/`rulesArabic` الميتين → **0 مشاكل**.
3. تمت مطابقة أسماء الحقول التي تم إصلاحها/إضافتها (`titleArabic`, `structures[].label`, `commonUsage`, `commonMistakes[].note`) مع ما يقرأه `src/lib/queries.ts` فعلياً من قاعدة البيانات — تأكيد أنها ستظهر في الواجهة الحقيقية وليست حقولاً ميتة جديدة.
4. تم فحص `git diff` على عينات من كل خطوة (أيام 04, 07, 11, 15, 27, 28, 43, 50) للتأكد أن كل تعديل محصور تماماً في الحقل المستهدف دون أي أثر جانبي على محتوى آخر.

---

## 5. ملخص الأرقام

| البند | العدد |
|---|---|
| ملفات `content/day-XX.json` المعدَّلة | 47 من أصل 50 |
| عناصر `commonMistakes` تم إصلاح مفتاحها (`explanation → note`) | 16 |
| قيم `label` جديدة أُضيفت لعناصر `structures` | 150 |
| عناصر ترحّلت من `rules`/`rulesArabic` الميتة إلى `commonUsage` | 98 |
| عناصر `commonUsage` جديدة كُتبت من الصفر (الأيام 11-27) | 65 |
| قيم `titleArabic` جديدة كُتبت من الصفر (الأيام 28-50) | 24 |
| إجمالي أسطر `git diff` عبر `content/` | 488 إضافة / 355 حذف |
| نتيجة `validate-content.mjs` النهائية | PASS — 0 أخطاء، 0 تحذيرات |

---

## 6. ملاحظات ختامية

- كل التعديلات محصورة في `content/day-01.json` حتى `content/day-50.json`. لم يُعدَّل أي ملف في `src/` أو `scripts/` (ملفات الفحص المساعدة المؤقتة التي استُخدمت أثناء التنفيذ حُذفت بعد الانتهاء منها).
- الأيام 51-90 غير موجودة بعد في `content/` وخارج نطاق هذه المراجعة تماماً.
- التوصية الوحيدة المتبقية خارج نطاق هذه المهمة: تحديث `scripts/validate-content.mjs` ليتحقق أيضاً من `label`, `note`, `titleArabic`, و`commonUsage` على مستوى الحقول الفرعية، حتى لا تتكرر مشاكل مشابهة بصمت في محتوى الأيام القادمة (51-90) دون أن يلتقطها الفحص الآلي.
